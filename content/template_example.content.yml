# Import example demonstrating template usage.
---
# Generate randomized articles.
'#preprocess':
  - '#plugin': 'template_processor'
    # Indicate the template to be loaded matching <template>.template.yml.
    template: 'article'
    # Indicate the module to look in for the template.
    module: 'yaml_content'
    # Indicate how many times the template should be used to build content.
    count: 5
